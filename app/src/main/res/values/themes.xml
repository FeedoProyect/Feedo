<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProyectoFeedo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>

    <style name="Theme.ProyectoFeedo" parent="Base.Theme.ProyectoFeedo" />

    <style name="CustomTabLayout" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabBackground">@drawable/tab_background_selector</item>
        <item name="tabIndicatorHeight">0dp</item> <!-- Ocultamos el indicador por defecto -->
        <item name="tabTextColor">@color/color_text_tabs_perfil</item> <!-- Color del texto normal -->
        <item name="tabSelectedTextColor">@android:color/white</item> <!-- Texto cuando está seleccionado -->
        <item name="tabPaddingStart">16dp</item>
        <item name="tabPaddingEnd">16dp</item>
        <item name="tabPaddingTop">8dp</item>
        <item name="tabPaddingBottom">8dp</item>
        <item name="android:fontFamily">@font/balo_thambi</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>